<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        line-height: 1.6;
        color: #d1d1e0;
        background-color: #212240;
        margin: 0;
        padding: 0;
      }
      .email-container {
        max-width: 600px;
        margin: 40px auto;
        background-color: #2a2b4d;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 10px 25px rgba(0,0,0,0.3);
      }
      .email-header {
        background: linear-gradient(89.8deg, #0BB1D3 0%, #9F2FFF 100%);
        padding: 40px 20px;
        text-align: center;
      }
      .email-logo {
        font-size: 32px;
        font-weight: 800;
        color: #ffffff;
        margin: 0;
        letter-spacing: 3px;
        text-shadow: 0 2px 4px rgba(0,0,0,0.2);
      }
      .email-body {
        padding: 40px 30px;
      }
      .email-content {
        font-size: 16px;
        color: #d1d1e0;
      }
      .email-button {
        display: inline-block;
        padding: 16px 32px;
        margin: 24px 0;
        background: linear-gradient(89.8deg, #0BB1D3 0%, #9F2FFF 100%);
        color: #ffffff !important;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 700;
        text-align: center;
        box-shadow: 0 4px 15px rgba(11, 177, 211, 0.4);
      }
      .email-footer {
        background-color: #1a1b3a;
        padding: 30px;
        text-align: center;
        font-size: 14px;
        color: #717285;
        border-top: 1px solid #3a3b5c;
      }
      .email-footer a {
        color: #0BB1D3;
        text-decoration: none;
      }
      h1 {
        color: #ffffff;
        font-size: 28px;
        margin-bottom: 24px;
        font-weight: 700;
      }
      h2, h3 {
        color: #ffffff;
        margin-top: 24px;
      }
      p {
        margin: 16px 0;
      }
      .highlight {
        background-color: rgba(0, 81, 179, 0.2);
        padding: 20px;
        border-left: 4px solid #0BB1D3;
        margin: 25px 0;
        border-radius: 4px;
      }
      .critical-alert {
        background-color: rgba(255, 65, 156, 0.1);
        padding: 15px;
        border: 1px dashed #FF419C;
        border-radius: 8px;
        color: #FF419C;
        margin: 20px 0;
      }
      strong {
        color: #ffffff;
      }
      @media only screen and (max-width: 600px) {
        .email-container {
          margin: 0 auto;
          border-radius: 0;
        }
        .email-body {
          padding: 30px 20px;
        }
        h1 {
          font-size: 24px;
        }
      }
    </style>
  </head>

  <body>
    <div class="email-container">
      <div class="email-header">
        <h2 class="email-logo">🚗 JOMBO</h2>
      </div>
      
      <div class="email-body">
        <%= yield %>
      </div>
      
      <div class="email-footer">
        <p>© <%= Time.current.year %> Jombo. Todos los derechos reservados.</p>
        <p>
          <%= t('layouts.mailer.platform_description', default: 'Plataforma de carpooling inteligente') %><br>
          <a href="#"><%= t('layouts.mailer.terms', default: 'Términos') %></a> | 
          <a href="#"><%= t('layouts.mailer.privacy', default: 'Privacidad') %></a> | 
          <a href="#"><%= t('layouts.mailer.contact', default: 'Contacto') %></a>
        </p>
      </div>
    </div>
  </body>
</html>
